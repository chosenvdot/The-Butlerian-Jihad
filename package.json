{
  "name": "victor-brasil-site",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Victor Brasil — personal site (photography, engineering CV, journal).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.39.2",
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.0",
    "wrangler": "^4.97.0"
  }
}
